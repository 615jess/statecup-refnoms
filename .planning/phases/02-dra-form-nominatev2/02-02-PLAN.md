---
phase: 02-dra-form-nominatev2
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - spring-state-cup-nomination.html
autonomous: false

must_haves:
  truths:
    - "DRA selects their name and district/email auto-fill — same as v1.0 but with additional entries for Don Eubank, Mark Herrington, SYRA, and Assignor"
    - "Each referee card has exactly 6 fields: first name (required), last name (required), email (required), max age as AR (required dropdown U12-U19), max age as referee (required dropdown U12-U19), DRA notes (optional textarea)"
    - "Spreadsheet upload appends to existing manual entries instead of replacing them"
    - "Upload template has 6 columns matching the 6-field set"
    - "Form POSTs action: 'nominateV2' with rows array containing dra, dra_email, district, ref_num, first, last, ref_email, max_ar, max_ref, notes"
    - "Success summary shows each nominee with (new) or (updated) badge based on backend response"
    - "Error state includes assignor email (jerickson@tnsoccer.org) for fallback contact"
  artifacts:
    - path: "spring-state-cup-nomination.html"
      provides: "v2.0 DRA nomination form with simplified fields, SheetJS upload, and nominateV2 submit"
      contains: "nominateV2"
      min_lines: 400
  key_links:
    - from: "spring-state-cup-nomination.html (fetch)"
      to: "Apps Script /exec URL"
      via: "fetch(SHEET_URL, { method: 'POST', body: JSON.stringify(payload) })"
      pattern: "action.*nominateV2"
    - from: "spring-state-cup-nomination.html (summary)"
      to: "Backend response.results"
      via: "results.forEach with status new/updated badges"
      pattern: "status.*===.*new|status.*===.*updated"
    - from: "spring-state-cup-nomination.html (upload)"
      to: "SheetJS XLSX library"
      via: "XLSX.read and XLSX.utils.sheet_to_json"
      pattern: "XLSX\\.read|XLSX\\.utils"
---

<objective>
Adapt the v1.0 DRA nomination form (spring-state-cup-nomination.html) into v2.0: strip removed fields (phone, age, availability, hotel, day notes), keep 6 fields per referee, update spreadsheet upload to append mode with simplified template, change submit payload to nominateV2 format, and update success summary to show new/updated badges from backend response.

Purpose: This is the user-facing form that DRAs interact with. It sends nominations to the nominateV2 backend deployed in Plan 02-01.
Output: Updated `spring-state-cup-nomination.html` ready for GitHub Pages deployment.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/COLUMN-MAP.md
@.planning/phases/02-dra-form-nominatev2/02-RESEARCH.md
@.planning/phases/02-dra-form-nominatev2/02-CONTEXT.md
@.planning/phases/02-dra-form-nominatev2/02-01-SUMMARY.md
@spring-state-cup-nomination.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite spring-state-cup-nomination.html for v2.0</name>
  <files>spring-state-cup-nomination.html</files>
  <action>
Rewrite the existing v1.0 form into the v2.0 DRA nomination form. The visual design (CSS, layout, header, topbar, colors, card pattern) stays the same. The structural changes are:

**SHEET_URL constant:** Replace the v1.0 URL with the new /exec URL from Plan 02-01 SUMMARY (look for the deployment URL the user provided during the checkpoint). If the URL is in the summary, use it. If not, leave the existing setup-callout pattern asking the user to replace it.

**DRA dropdown (Section 1):** Keep the existing 7 DRA entries. Add these new entries (placeholder emails/districts — leave TODO comments for the user to fill in real values):
- Don Eubank — with TODO comment for email and district
- Mark Herrington (SRA) — with TODO comment for email
- SYRA — with TODO comment for name, email
- Jess Erickson (Assignor) — jerickson@tnsoccer.org, Assignor

Keep the auto-fill behavior for district and email fields (same `change` event handler pattern).

**AGES constant:** Change from `['U12','U13','U14','U15','U16','U17','U18','U19','Adult / Open']` to `['U12','U13','U14','U15','U16','U17','U18','U19']`. Remove 'Adult / Open'.

**Referee card (addRef function) — 6 fields only:**
```
Row 1: First Name (required, text)  |  Last Name (required, text)
Row 2: Email (required, email) — span full width (s2 class)
Row 3: Max Age as AR (required, dropdown U12-U19)  |  Max Age as Referee (required, dropdown U12-U19)
        Use the styled cap-grid / cap-blk pattern from v1.0 for the age dropdowns
Row 4: DRA Notes (optional, textarea)
```

Remove entirely from the referee card:
- Phone field
- Referee Age field (number input)
- Weekend Availability checkboxes and hotel sub-toggles
- Day-Specific Limitations text input
- The separator lines between those removed sections

Email field: Change from optional (no `required` attribute in v1.0) to required. Add `<span class="req">*</span>` to its label.

Max Age dropdowns: Add `required` attribute. These use the `ageOpts()` function which generates options from the AGES array.

Keep the navy header bar per referee card with referee number and remove button (same as v1.0).

**Validation (submit handler):** Update the field suffix list for validation. v1.0 validates `['_fn','_ln','_age']`. v2.0 validates `['_fn','_ln','_em']`. Also validate that both age dropdowns have a selection (check `_mar` and `_mrf` selects are not empty string).

**Submit payload:** Change from v1.0 format to:
```javascript
const payload = {
  action: 'nominateV2',
  website: document.getElementById('hp-field').value,
  rows: []
};
```

Each row object:
```javascript
{
  dra: draName,
  dra_email: draEmail,
  district: draDist,
  ref_num: i + 1,
  first: g('_fn'),
  last: g('_ln'),
  ref_email: g('_em'),
  max_ar: g('_mar'),
  max_ref: g('_mrf'),
  notes: g('_nt')
}
```

Note: `g()` function reads from name suffix. `_mar` and `_mrf` read from `<select>` elements. Ensure the `g()` helper works with both input and select elements (v1.0's `g()` uses querySelector `[name$="..."]` which works for both — keep this pattern).

**fetch call:** Keep the exact same pattern — `fetch(SHEET_URL, { method: 'POST', body: JSON.stringify(payload) })`. Do NOT add Content-Type header (CORS issue).

**Success summary:** Replace v1.0 summary format with v2.0 format. The backend now returns `{ ok: true, results: [{ name, status }] }`. Parse the response JSON. Build summary HTML:
- Header: "DRA: {draName} -- {draDist} District" (same as v1.0)
- For each result: Show name with (new) or (updated) badge:
  ```
  <div class="sum-ref">
    <strong>{name}</strong> <span style="...">(new)</span>
  </div>
  ```
  - "(new)" badge: color var(--green), font-size 11px
  - "(updated)" badge: color var(--muted), font-size 11px

Use the `esc()` XSS helper on all user-provided strings in the summary (already exists in v1.0).

**Error message:** Update the error text to use the assignor email: `'Submission failed: ' + err.message + '. Please retry or contact Jess Erickson (jerickson@tnsoccer.org) directly.'` (same text as v1.0 — just verify it's still there).

**TEMPLATE_HEADERS (spreadsheet upload):** Change from v1.0's 13 headers to v2.0's 6:
```javascript
const TEMPLATE_HEADERS = [
  'First Name', 'Last Name', 'Email',
  'Max Age as AR', 'Max Age as Referee',
  'DRA Notes'
];
```

**downloadTemplate():** Update example row to match 6 columns:
```javascript
['Jane', 'Smith', 'jane@example.com', 'U17', 'U19', 'Strong center referee']
```

**handleUpload() — append mode:** Remove the two lines that clear existing entries:
```javascript
// REMOVE these lines:
// document.getElementById('ref-list').innerHTML = '';
// rc = 0;
```
This makes upload append to any existing manual entries instead of replacing them.

Update the field-setting block in handleUpload to match the 6-field set:
```javascript
setVal(f, `r${n}_fn`, val(row, col('First Name')));
setVal(f, `r${n}_ln`, val(row, col('Last Name')));
setVal(f, `r${n}_em`, val(row, col('Email')));
setSelect(f, `r${n}_mar`, val(row, col('Max Age as AR')));
setSelect(f, `r${n}_mrf`, val(row, col('Max Age as Referee')));
setTextarea(f, `r${n}_nt`, val(row, col('DRA Notes')));
```

Remove all the weekend availability, hotel, phone, age, and day notes field-setting lines.

**Upload validation:** Required columns for validation: `['First Name', 'Last Name', 'Email']`. Remove 'Referee Age' from the required list (it no longer exists).

Remove the per-row age validation (`parseInt(val(...), 10)` block). Replace with email presence check if desired, or simply rely on the required columns check.

**Upload result message:** Update the count text. When upload appends, show how many were added: `"X referees loaded from {filename}"`. If entries already existed before upload, consider showing total: `"X referees loaded from {filename} (Y total nominees)"`. Claude's discretion on exact wording.

**clearUpload():** This function currently clears and re-adds one empty card. In append mode, it should: clear only the uploaded entries (or just reset the whole list — Claude's discretion since the user can always re-add manually). Simplest: keep current behavior (clears all, adds one blank card).

**Remove toggleHotel function:** No longer needed (no hotel checkboxes in v2.0 cards).

**Remove unused CSS:** The following CSS classes are no longer used and can be removed to keep the file clean:
- `.avail-grid`, `.avail-opt`, `.av-wk`, `.av-dt`, `.chk-box` (availability checkboxes)
- `.hotel-row`, `.hotel-lbl`, `input[type=checkbox].hotel-input` (hotel toggles)

Or keep them — they don't hurt anything. Claude's discretion.

**Keep unchanged:**
- All CSS variables, topbar, header, hero, dates-row, footer
- Honeypot field
- `esc()` XSS helper
- SheetJS CDN link with SRI hash (exact same URL and integrity attribute)
- Upload zone visual design and drag-and-drop handlers
- Or-divider pattern
- File size limit (5 MB)
- Spinner animation on submit button
- Setup callout (if SHEET_URL still needs replacement)

**Post-submit behavior:** After successful submission, keep the same pattern: show green success box, scroll to it, re-enable submit button. Do NOT reset the form (user might want to verify what was submitted). This matches v1.0 behavior.
  </action>
  <verify>
1. Open the file in a browser — verify it renders with the same visual design as v1.0
2. Check DRA dropdown has all 7 original entries plus the new entries (with TODO comments for missing values)
3. Click "Add Referee" — verify the card has exactly 6 fields (first name, last name, email, max age AR, max age ref, DRA notes)
4. Verify no phone, referee age, availability, hotel, or day notes fields appear
5. Check age dropdowns show U12 through U19 only (no "Adult / Open")
6. Check email field has required indicator (red asterisk)
7. Click "Download Template" — verify the downloaded file has 6 columns
8. Grep the file for 'nominateV2' to confirm the action field is present in the payload
9. Grep for 'Adult / Open' to confirm it's been removed
10. Grep for 'toggleHotel' — should be removed or unused
11. Verify the upload handler does NOT contain the lines that clear ref-list (append mode)
  </verify>
  <done>
spring-state-cup-nomination.html is a complete v2.0 DRA nomination form with: 6 fields per referee, required email, U12-U19 age dropdowns, append-mode upload with simplified template, nominateV2 payload format, and new/updated summary badges. Visual design matches v1.0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification of DRA form + backend</name>
  <what-built>
The complete Phase 2 DRA nomination flow: v2.0 form that POSTs to nominateV2 backend, with simplified fields, email deduplication, token generation, and success summary showing new/updated status per nominee.
  </what-built>
  <how-to-verify>
**Prerequisite:** The SHEET_URL in the HTML file must point to the deployed /exec URL from Plan 02-01. If it still says the old v1.0 URL or a placeholder, update it now.

**Test 1 — Single manual nomination:**
1. Open spring-state-cup-nomination.html in a browser
2. Select a DRA name — verify district and email auto-fill
3. Fill in a referee: first name, last name, email, select max ages, add a note
4. Click "Submit Nominations"
5. Verify green success box appears with the referee name and "(new)" badge
6. Check the Google Sheet: one new row with A-H and Q filled, I-P blank, R has UUID, S says "Not Sent"

**Test 2 — Duplicate email (update):**
1. Reload the form
2. Submit the SAME referee email with a different DRA name or note
3. Verify success box shows "(updated)" badge
4. Check the sheet: still only ONE row for that email, DRA fields updated, columns I-P and R unchanged

**Test 3 — Spreadsheet upload (append mode):**
1. Click "Download Template" — verify 6-column .xlsx file downloads
2. Fill in 2-3 referees in the template (use different emails)
3. Upload the file — verify referee cards appear BELOW any existing manual entries (append, not replace)
4. Add one more referee manually after the upload
5. Submit all — verify success summary lists all nominees

**Test 4 — Within-batch dedup:**
1. Create a spreadsheet with the same email appearing twice
2. Upload and submit
3. Verify only ONE row per email in the sheet (not duplicates)

**Test 5 — Visual check:**
1. Verify the form looks the same as v1.0 (colors, fonts, layout, header)
2. Check mobile responsiveness (resize browser to narrow width)
3. Verify no phone/age/availability/hotel fields appear anywhere

After testing, delete all test rows from the Google Sheet.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found during testing</resume-signal>
</task>

</tasks>

<verification>
- spring-state-cup-nomination.html renders correctly in browser with v2.0 fields
- DRA dropdown includes original 7 entries plus new entries
- Referee cards have exactly 6 fields (first name, last name, email, max age AR, max age ref, notes)
- Age dropdowns show U12-U19 only
- Upload appends to existing entries
- Template download has 6 columns
- Form submits with action: 'nominateV2' payload
- Success summary shows (new) and (updated) badges from backend response
- End-to-end flow works: form -> backend -> sheet row with token
- Duplicate email produces update (not new row)
- Visual design matches v1.0 (Open Sans, navy/red/gold)
</verification>

<success_criteria>
1. DRA submits a nomination with 6 fields -- row appears in sheet with A-H, Q filled, I-P blank, R with UUID, S "Not Sent"
2. Re-nominating same email updates existing row's DRA fields without duplicate or overwriting I-P/R/S
3. Spreadsheet upload with multiple referees creates correct rows with no duplicates
4. Upload appends to manual entries (not replaces)
5. Success summary shows new vs. updated status per nominee
6. Form visual design matches v1.0 style
</success_criteria>

<output>
After completion, create `.planning/phases/02-dra-form-nominatev2/02-02-SUMMARY.md`
</output>
