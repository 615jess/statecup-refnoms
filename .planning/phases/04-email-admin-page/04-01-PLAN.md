---
phase: 04-email-admin-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/adminemail.gs
  - scripts/refdetails.gs
  - scripts/nominatev2.gs
autonomous: true

must_haves:
  truths:
    - "doGet with action=getAllNominees returns all nominee rows as a JSON array"
    - "doPost with action=markSent updates Status to Sent and writes SentAt timestamp for a Not Sent referee"
    - "markSent is idempotent — calling it on an already-Sent or Confirmed referee returns ok:true without overwriting"
    - "Existing doGet (token-based) and doPost (nominateV2, submitDetails) still work unchanged"
  artifacts:
    - path: "scripts/adminemail.gs"
      provides: "_handleGetAllNominees and _handleMarkSent functions, COL_SENT_AT constant"
      contains: "_handleGetAllNominees"
    - path: "scripts/refdetails.gs"
      provides: "doGet routing for action=getAllNominees"
      contains: "getAllNominees"
    - path: "scripts/nominatev2.gs"
      provides: "doPost routing for action=markSent"
      contains: "markSent"
  key_links:
    - from: "scripts/refdetails.gs doGet"
      to: "scripts/adminemail.gs _handleGetAllNominees"
      via: "action parameter routing"
      pattern: "action === 'getAllNominees'"
    - from: "scripts/nominatev2.gs doPost"
      to: "scripts/adminemail.gs _handleMarkSent"
      via: "action parameter routing"
      pattern: "action === 'markSent'"
    - from: "scripts/adminemail.gs _handleMarkSent"
      to: "scripts/refdetails.gs _findRowByToken"
      via: "GAS shared scope function call"
      pattern: "_findRowByToken\\(sheet, token\\)"
---

<objective>
Build the two Apps Script backend functions for Phase 4 (getAllNominees + markSent) and wire them into the existing doGet/doPost entry points.

Purpose: The admin page (Plan 02) needs a backend that returns all nominee data and can mark a referee's status as Sent. These endpoints follow the exact same patterns as Phase 3's getDetails and submitDetails.

Output: Three modified/new `.gs` files ready to be pasted into the Apps Script editor alongside the existing project code.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/COLUMN-MAP.md
@.planning/phases/04-email-admin-page/04-CONTEXT.md
@.planning/phases/04-email-admin-page/04-RESEARCH.md
@scripts/refdetails.gs
@scripts/nominatev2.gs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create adminemail.gs with _handleGetAllNominees and _handleMarkSent</name>
  <files>scripts/adminemail.gs</files>
  <action>
Create `scripts/adminemail.gs` — a new Apps Script file for Phase 4 admin email functions. This file does NOT declare doGet or doPost (those entry points are in refdetails.gs and nominatev2.gs). All functions here are callable from the existing entry points because GAS compiles all .gs files in a project into one scope.

**File header comment** matching the style in nominatev2.gs and refdetails.gs: explain what the file does, list column references used, note that doGet/doPost routing is in the other files.

**Constants:**
- Declare `var COL_SENT_AT = 20; // T` — this is the only column constant not yet declared in any .gs file. All other needed constants (COL_TOKEN, COL_STATUS, COL_FIRST_NAME, COL_LAST_NAME, COL_REF_EMAIL, COL_DRA_NAME) are in nominatev2.gs and shared via GAS scope. Do NOT redeclare them.

**Function: _handleGetAllNominees()**
- No parameters (called from doGet when action=getAllNominees)
- Get active spreadsheet and active sheet
- Read lastRow; if lastRow <= 1, return `_jsonResponse({ ok: true, nominees: [] })`
- Read all data rows in one call: `sheet.getRange(2, 1, lastRow - 1, 25).getValues()` — columns A through Y (25 cols)
- Also read tournament properties via `PropertiesService.getScriptProperties()`: ASSIGNOR_EMAIL, WEEKEND_1_DATES, WEEKEND_2_DATES, REF_FORM_URL
- Also read deadline display via `_getDeadlineState(ss)` (from refdetails.gs shared scope)
- Map each row to a JSON object using 0-based array indices (cross-referenced against COLUMN-MAP.md):
  - firstName: r[5] (F), lastName: r[6] (G), refEmail: r[7] (H)
  - draName: r[1] (B), status: r[18] (S), token: r[17] (R)
  - refWeekend1: r[21] (V), refWeekend2: r[22] (W)
- Cast all values to String with `|| ''` fallback (same pattern as _handleGetDetails)
- Return `_jsonResponse({ ok: true, nominees: [...], props: { assignorEmail, weekend1Dates, weekend2Dates, refFormUrl, deadlineDisplay } })`
- Include the props in the response so the admin page can build mailto bodies without hardcoding tournament details
- Add Logger.log with nominee count

**Function: _handleMarkSent(payload)**
- Accepts payload object with `payload.token` field
- Acquire script lock via `LockService.getScriptLock()` with 15-second waitLock (same as _handleSubmitDetails pattern)
- Validate token: trim, check non-empty, return error if missing
- Call `_findRowByToken(sheet, token)` (from refdetails.gs shared scope) — return error if -1
- Read current status: `sheet.getRange(rowNum, COL_STATUS).getValue()`
- **Idempotency check:** If currentStatus !== 'Not Sent', return `{ ok: true, alreadyMarked: true }` — do NOT overwrite Sent or Confirmed rows
- Write two cells:
  - `sheet.getRange(rowNum, COL_STATUS).setValue('Sent')` — column S = 19
  - `sheet.getRange(rowNum, COL_SENT_AT).setValue(new Date())` — column T = 20
- Logger.log with row number
- Return `{ ok: true, alreadyMarked: false }`
- Release lock in finally block (same pattern as Phase 3)

**Style:** Match the comment density and JSDoc style of refdetails.gs exactly. Every function gets a block comment explaining purpose, steps, parameters, and return value. Column references include both 1-based and 0-based where relevant.
  </action>
  <verify>
- File exists at `scripts/adminemail.gs`
- Contains `_handleGetAllNominees` function
- Contains `_handleMarkSent` function
- Contains `var COL_SENT_AT = 20;`
- Does NOT contain `function doGet` or `function doPost`
- Does NOT redeclare COL_TOKEN, COL_STATUS, COL_FIRST_NAME, COL_LAST_NAME, COL_REF_EMAIL, COL_DRA_NAME (these are in nominatev2.gs)
- Uses `_findRowByToken` (from refdetails.gs) without redeclaring it
- Uses `_jsonResponse` (from nominatev2.gs) without redeclaring it
- Uses `_getDeadlineState` (from refdetails.gs) without redeclaring it
  </verify>
  <done>adminemail.gs contains both handler functions with correct column indices matching COLUMN-MAP.md, proper LockService usage, idempotent markSent, and tournament props in getAllNominees response</done>
</task>

<task type="auto">
  <name>Task 2: Extend doGet and doPost routing for Phase 4 actions</name>
  <files>scripts/refdetails.gs, scripts/nominatev2.gs</files>
  <action>
**Modify refdetails.gs — extend doGet to route getAllNominees:**

The current doGet in refdetails.gs (lines 176-200) immediately checks for a token parameter. Add action-parameter routing BEFORE the token check:

```javascript
function doGet(e) {
  try {
    var action = (e && e.parameter && e.parameter.action) ? e.parameter.action : '';

    // Phase 4: Admin page — return all nominees
    if (action === 'getAllNominees') {
      return _handleGetAllNominees();
    }

    // Phase 3: Referee detail form — token-based lookup (existing behavior)
    var token = (e && e.parameter && e.parameter.token)
      ? e.parameter.token.trim()
      : '';

    if (!token) {
      return _jsonResponse({
        ok: false,
        error: 'missing_token',
        message: 'No token provided.'
      });
    }

    return _handleGetDetails(token);

  } catch (err) {
    Logger.log('doGet error: ' + err.message);
    return _jsonResponse({
      ok: false,
      error: 'server_error',
      message: 'An internal error occurred.'
    });
  }
}
```

The key change: check `e.parameter.action` first. If action=getAllNominees, delegate to _handleGetAllNominees (in adminemail.gs). Otherwise, fall through to the existing token-based lookup. This preserves 100% backward compatibility — the referee detail form sends `?token=XYZ` with no action parameter, so it hits the existing path.

Update the file header JSDoc to mention Phase 4 routing.

**Modify nominatev2.gs — extend doPost to route markSent:**

In the existing doPost function (lines 99-124), add a new `if` branch for `payload.action === 'markSent'` AFTER the honeypot check and BEFORE or alongside the existing action checks. Place it between the submitDetails check and the unknown-action fallback:

```javascript
// Phase 4: Admin page — mark referee as Sent
if (payload.action === 'markSent') {
  return _handleMarkSent(payload);
}
```

The final doPost routing order should be:
1. Honeypot check (existing)
2. nominateV2 (existing)
3. submitDetails (existing — Phase 3)
4. markSent (NEW — Phase 4)
5. Unknown action fallback (existing)

Update the file header JSDoc to mention Phase 4 routing.

**CRITICAL — Do not change anything else in these files.** The only modifications are:
- refdetails.gs: doGet function body (add action routing), header comment
- nominatev2.gs: doPost function body (add markSent branch), header comment
  </action>
  <verify>
- `scripts/refdetails.gs` doGet function checks `e.parameter.action` before checking `e.parameter.token`
- `scripts/refdetails.gs` doGet routes `action === 'getAllNominees'` to `_handleGetAllNominees()`
- `scripts/refdetails.gs` doGet still handles token-based lookup when no action parameter is present
- `scripts/nominatev2.gs` doPost routes `payload.action === 'markSent'` to `_handleMarkSent(payload)`
- `scripts/nominatev2.gs` doPost still routes nominateV2 and submitDetails correctly
- No other functions in either file are modified
  </verify>
  <done>doGet routes getAllNominees to adminemail.gs; doPost routes markSent to adminemail.gs; all existing Phase 2 and Phase 3 routes work unchanged</done>
</task>

</tasks>

<verification>
1. All three .gs files have consistent column constant declarations with no redeclarations
2. The action routing in doGet and doPost does not break existing behavior (token-based GET still works, nominateV2 and submitDetails POST still work)
3. _handleGetAllNominees returns the correct JSON shape with nominee array and props object
4. _handleMarkSent uses LockService, idempotency check, and writes both COL_STATUS and COL_SENT_AT
5. No function name conflicts across the three .gs files
</verification>

<success_criteria>
- adminemail.gs exists with _handleGetAllNominees and _handleMarkSent
- refdetails.gs doGet routes action=getAllNominees before falling through to token lookup
- nominatev2.gs doPost routes action=markSent alongside nominateV2 and submitDetails
- Column indices in adminemail.gs match COLUMN-MAP.md exactly
- No functions are redeclared across files
</success_criteria>

<output>
After completion, create `.planning/phases/04-email-admin-page/04-01-SUMMARY.md`
</output>
