---
phase: 05-pre-go-live-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spring-state-cup-nomination.html
  - scripts/nominatev2.gs
  - referee-details.html
  - admin.html
autonomous: false

must_haves:
  truths:
    - "DRA dropdown shows real email addresses for Don Eubank (SRA) and Mark Herrington (SYRA) — no TODO_ prefixes"
    - "setTournamentConstants() source code contains the actual GitHub Pages referee-details URL"
    - "All three HTML pages reference the same single GAS deployment URL"
    - "Cell Z1 (ConfirmationDeadline) contains the actual tournament response deadline date"
  artifacts:
    - path: "spring-state-cup-nomination.html"
      provides: "DRA dropdown with real emails, consolidated deployment URL"
      contains: "Don Eubank|REAL_EMAIL|SRA"
    - path: "scripts/nominatev2.gs"
      provides: "REF_FORM_URL matching runtime value"
      contains: "615jess.github.io"
    - path: "referee-details.html"
      provides: "Consolidated deployment URL"
    - path: "admin.html"
      provides: "Consolidated deployment URL"
  key_links:
    - from: "spring-state-cup-nomination.html"
      to: "GAS deployment"
      via: "SHEET_URL constant (line 294)"
      pattern: "SHEET_URL\\s*=\\s*[\"']https://script\\.google\\.com"
    - from: "referee-details.html"
      to: "GAS deployment"
      via: "SCRIPT_URL constant (line 390)"
      pattern: "SCRIPT_URL\\s*=\\s*['\"]https://script\\.google\\.com"
    - from: "admin.html"
      to: "GAS deployment"
      via: "SCRIPT_URL constant (line 259)"
      pattern: "SCRIPT_URL\\s*=\\s*['\"]https://script\\.google\\.com"
---

<objective>
Resolve all 4 tech debt items from the v2.0 milestone audit so the system is production-ready.

Purpose: The system currently works but contains placeholder values (fake emails, TBD URLs) and deployment fragmentation that must be cleaned up before real DRAs and referees use it.

Output: All three HTML files and nominatev2.gs updated with real production values; Google Sheet deadline date set.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v2.0-MILESTONE-AUDIT.md
@.planning/phases/05-pre-go-live-cleanup/05-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Collect production values from user</name>
  <decision>Four production values are needed before code edits can proceed. All four are user-only knowledge that cannot be derived from the codebase.</decision>
  <context>
The v2.0 audit identified 4 tech debt items. Three require values only the user knows, and one requires a manual GAS deployment step.

**Current placeholders in code:**
- `spring-state-cup-nomination.html` line 221: `TODO_eubank@email.com`
- `spring-state-cup-nomination.html` line 223: `TODO_herrington@email.com`
- `scripts/nominatev2.gs` line 437: `'TBD — set to GitHub Pages URL before Phase 4'`
- Three HTML files each point to different GAS deployment URLs

**GitHub Pages URL (confirm or correct):** `https://615jess.github.io/statecup-refnoms/referee-details.html`
  </context>
  <options>
    <option id="provide-values">
      <name>Provide all 4 values</name>
      <pros>Plan can proceed to completion in one pass</pros>
      <cons>None — all values are required</cons>
    </option>
  </options>
  <resume-signal>
Please provide these 4 values:

1. **Don Eubank (SRA) email address** — replaces TODO_eubank@email.com in DRA dropdown
2. **Mark Herrington (SYRA) email address** — replaces TODO_herrington@email.com in DRA dropdown
3. **New GAS deployment URL** — You must create a new deployment in Apps Script (Extensions > Apps Script > Deploy > New deployment > Web app > Execute as Me > Anyone can access). Paste the full URL here. This single URL will replace the 3 different URLs currently in the HTML files.
4. **Tournament response deadline date** — The date to enter in cell Z1 (ConfirmationDeadline). Tournament weekends are May 16-17 and May 23-24, 2026. What date should referees respond by?

Also confirm: Is the GitHub Pages referee-details URL `https://615jess.github.io/statecup-refnoms/referee-details.html`? (If not, provide the correct URL.)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply all production values to source code</name>
  <files>
    spring-state-cup-nomination.html
    scripts/nominatev2.gs
    referee-details.html
    admin.html
  </files>
  <action>
Using the values provided by the user in Task 1, make these 4 targeted edits:

**Edit 1 — DRA placeholder emails (spring-state-cup-nomination.html):**
- Line 220-221: Remove the TODO comment and replace `TODO_eubank@email.com` with the real email in the option value. Keep the pipe-delimited format: `Don Eubank|REAL_EMAIL|SRA`
- Line 222-223: Remove the TODO comment and replace `TODO_herrington@email.com` with the real email. Keep format: `Mark Herrington|REAL_EMAIL|SYRA`

**Edit 2 — REF_FORM_URL source code (scripts/nominatev2.gs):**
- Line 436-437: Replace the TODO comment and TBD placeholder with the confirmed GitHub Pages URL. Result should be:
  `'REF_FORM_URL': 'https://615jess.github.io/statecup-refnoms/referee-details.html'`
  (or whatever URL the user confirmed)

**Edit 3 — Deployment URL consolidation (all 3 HTML files):**
- `spring-state-cup-nomination.html` line 294: Replace the SHEET_URL value with the new deployment URL. Keep the variable name as SHEET_URL (do NOT rename it to SCRIPT_URL).
- `referee-details.html` line 390: Replace the SCRIPT_URL value with the new deployment URL.
- `admin.html` line 259: Replace the SCRIPT_URL value with the new deployment URL.

**Important:** Each file uses its own variable name (SHEET_URL vs SCRIPT_URL). Do NOT change variable names — only change the URL string values.

Do NOT touch any other lines in these files.
  </action>
  <verify>
Run these checks to confirm all edits:

```bash
# 1. No TODO_ emails remain
grep -n "TODO_" spring-state-cup-nomination.html
# Expected: no output

# 2. No TBD in nominatev2.gs REF_FORM_URL
grep -n "TBD" scripts/nominatev2.gs
# Expected: no output

# 3. REF_FORM_URL contains github.io
grep -n "REF_FORM_URL" scripts/nominatev2.gs
# Expected: line with github.io URL

# 4. All 3 HTML files have the same deployment URL
grep -oP "https://script\.google\.com/macros/s/[^'\"]*" spring-state-cup-nomination.html referee-details.html admin.html | sort -u
# Expected: exactly 1 unique URL

# 5. Variable names preserved
grep -n "SHEET_URL" spring-state-cup-nomination.html
grep -n "SCRIPT_URL" referee-details.html admin.html
# Expected: SHEET_URL in nomination form, SCRIPT_URL in the other two
```
  </verify>
  <done>
- spring-state-cup-nomination.html DRA dropdown has real emails for Don Eubank and Mark Herrington (no TODO_ prefixes)
- scripts/nominatev2.gs setTournamentConstants has the actual GitHub Pages URL (no TBD)
- All three HTML files reference the same single GAS deployment URL
- Variable names unchanged (SHEET_URL in nomination form, SCRIPT_URL in other two)
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Set tournament deadline date in Google Sheet</name>
  <action>This is a Google Sheet cell edit that Claude cannot perform — it requires accessing the Google Sheet directly.</action>
  <instructions>
Enter the tournament response deadline date in cell Z1 of the production Google Sheet.

1. Open the State Cup Referee Nominations Google Sheet
2. Navigate to cell Z1 (which has the named range "ConfirmationDeadline")
3. Enter the deadline date you provided in Task 1 (format: a valid date that Google Sheets recognizes, e.g., 5/9/2026)
4. Verify the cell shows a date value (not text)
  </instructions>
  <resume-signal>Type "done" after entering the deadline date in Z1, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
All 4 tech debt items from v2.0-MILESTONE-AUDIT.md are resolved:

1. DRA placeholder emails: `grep "TODO_" spring-state-cup-nomination.html` returns nothing
2. REF_FORM_URL source mismatch: `grep "TBD" scripts/nominatev2.gs` returns nothing
3. Deployment URL consolidation: All 3 HTML files contain the same GAS deployment URL
4. ConfirmationDeadline: User confirmed Z1 contains the deadline date

No remaining TODO comments related to go-live blockers in any source file.
</verification>

<success_criteria>
- Zero TODO_ placeholder emails in spring-state-cup-nomination.html
- Zero TBD values in scripts/nominatev2.gs
- Exactly 1 unique GAS deployment URL across all 3 HTML files
- User confirmed deadline date entered in Z1
- All changes committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/05-pre-go-live-cleanup/05-01-SUMMARY.md`
</output>
