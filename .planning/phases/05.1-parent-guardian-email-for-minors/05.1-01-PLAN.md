---
phase: 05.1-parent-guardian-email-for-minors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/refdetails.gs
  - scripts/adminemail.gs
  - scripts/setup-schema-v2.gs
  - .planning/COLUMN-MAP.md
autonomous: true

must_haves:
  truths:
    - "Column AB header 'Parent/Guardian Email' exists in the sheet schema setup"
    - "COL_PARENT_EMAIL = 28 constant is declared in refdetails.gs"
    - "GET details endpoint returns parentEmail field (read from column AB, 0-based index 27)"
    - "POST submitDetails writes parentEmail to column AB when present in payload"
    - "getAllNominees returns parentEmail and age for each nominee"
    - "COLUMN-MAP.md documents column AB"
  artifacts:
    - path: "scripts/refdetails.gs"
      provides: "COL_PARENT_EMAIL constant, parentEmail in getDetails response, parentEmail write in submitDetails"
      contains: "COL_PARENT_EMAIL"
    - path: "scripts/adminemail.gs"
      provides: "parentEmail and age fields in nominee objects"
      contains: "parentEmail"
    - path: "scripts/setup-schema-v2.gs"
      provides: "Column AB header in HEADERS_V2 array"
      contains: "Parent/Guardian Email"
    - path: ".planning/COLUMN-MAP.md"
      provides: "Column AB documentation"
      contains: "Parent/Guardian Email"
  key_links:
    - from: "scripts/refdetails.gs (_handleGetDetails)"
      to: "column AB (index 27)"
      via: "rowData[27] read in getValues range"
      pattern: "parentEmail.*rowData\\[27\\]"
    - from: "scripts/refdetails.gs (_handleSubmitDetails)"
      to: "column AB (COL_PARENT_EMAIL = 28)"
      via: "setValue on COL_PARENT_EMAIL"
      pattern: "COL_PARENT_EMAIL.*setValue"
    - from: "scripts/adminemail.gs (_handleGetAllNominees)"
      to: "column AB (index 27)"
      via: "nominee object mapping"
      pattern: "parentEmail.*r\\[27\\]"
---

<objective>
Add Parent/Guardian Email backend support: column constant, schema header, read/write in refdetails.gs endpoints, expose in admin getAllNominees, and update COLUMN-MAP.md.

Purpose: The backend must support storing and retrieving a parent/guardian contact email for minor referees (age < 18) before the frontend can use it.
Output: Updated .gs files with COL_PARENT_EMAIL = 28, getDetails returning parentEmail, submitDetails writing parentEmail, getAllNominees returning parentEmail + age, schema including AB header, and updated column map.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/COLUMN-MAP.md
@scripts/refdetails.gs
@scripts/adminemail.gs
@scripts/setup-schema-v2.gs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add COL_PARENT_EMAIL constant and update refdetails.gs endpoints</name>
  <files>scripts/refdetails.gs</files>
  <action>
  1. Add column constant after COL_REF_NOTES (line 74):
     ```
     var COL_PARENT_EMAIL = 28; // AB
     ```

  2. Update `_handleGetDetails` — expand the getValues range from 25 columns to 28 columns so it reads through column AB:
     - Change `sheet.getRange(rowNum, 1, 1, 25).getValues()[0]` to `sheet.getRange(rowNum, 1, 1, 28).getValues()[0]`
     - Update the comment from "columns A through Y (25 columns)" to "columns A through AB (28 columns)"
     - Add `parentEmail` to the response object after `refNotes`:
       ```
       parentEmail:  String(rowData[27] || ''), // AB (0-based index 27)
       ```

  3. Update `_handleSubmitDetails` — after the system columns write (Step 8, after line 400), add a single-cell write for parentEmail:
     ```
     // Step 9: Write parent/guardian email (column AB) — only present for minors.
     sheet.getRange(rowNum, COL_PARENT_EMAIL).setValue(payload.parentEmail || '');
     ```

  4. Update the file header comment block:
     - Add `AB = col 28 (0-based 27) Parent/Guardian Email` to the COLUMN REFERENCES USED section
     - Add COL_PARENT_EMAIL to the COLUMN CONSTANTS IN THIS FILE section
  </action>
  <verify>
  Manually review the file:
  - `grep 'COL_PARENT_EMAIL' scripts/refdetails.gs` returns the constant declaration
  - `grep 'parentEmail' scripts/refdetails.gs` returns lines in _handleGetDetails response and _handleSubmitDetails write
  - `grep '1, 28' scripts/refdetails.gs` confirms the expanded getValues range
  </verify>
  <done>
  - COL_PARENT_EMAIL = 28 declared in refdetails.gs
  - _handleGetDetails reads 28 columns and returns parentEmail from rowData[27]
  - _handleSubmitDetails writes payload.parentEmail to COL_PARENT_EMAIL
  - File header comments updated with AB column reference
  </done>
</task>

<task type="auto">
  <name>Task 2: Update adminemail.gs, setup-schema-v2.gs, and COLUMN-MAP.md</name>
  <files>scripts/adminemail.gs, scripts/setup-schema-v2.gs, .planning/COLUMN-MAP.md</files>
  <action>
  **adminemail.gs — _handleGetAllNominees:**

  1. Expand the getValues range from 25 columns to 28 columns:
     - Change `sheet.getRange(2, 1, dataRowCount, 25).getValues()` to `sheet.getRange(2, 1, dataRowCount, 28).getValues()`
     - Update the comment from "A through Y (25 columns)" to "A through AB (28 columns)"

  2. Add `age` and `parentEmail` fields to the nominee object mapping (inside the `rows.map` function, after the `refWeekend2` line):
     ```
     age:          String(r[9]  || ''), // J (0-based index  9)
     parentEmail:  String(r[27] || '')  // AB (0-based index 27)
     ```

  3. Update the file header comment block:
     - Add `J = col 10 (0-based  9) Age` and `AB = col 28 (0-based 27) Parent/Guardian Email` to COLUMN REFERENCES USED

  **setup-schema-v2.gs:**

  1. Add the AB header to the HEADERS_V2 array. Currently the array ends at index 25 (Z = blank). Add after the Z entry:
     ```
     'Parent/Guardian Email' // AB col 28  (Phase 5.1 — minor referee contact)
     ```
     Note: The AA column header ("Confirmation Deadline:" label) is NOT in the HEADERS_V2 array — it's written separately in _rebuildHeaderRow. So AB becomes index 27 in the array (after Z at index 25, we need to also add AA = '' at index 26).

     IMPORTANT: Looking at the existing code, HEADERS_V2 has 26 entries (indices 0-25, columns A-Z). The AA label is written separately at line 211. To add AB properly:
     - Add `''` as index 26 (AA — label is written separately) and `'Parent/Guardian Email'` as index 27 (AB) to the array
     - Update the setValues range from 26 columns to 28 columns: change `sheet.getRange(1, 1, 1, 26).setValues([HEADERS_V2])` to `sheet.getRange(1, 1, 1, 28).setValues([HEADERS_V2])`
     - Update _clearDataRows range from 26 to 28 columns: change `sheet.getRange(2, 1, dataRowCount, 26).clearContent()` to `sheet.getRange(2, 1, dataRowCount, 28).clearContent()`

  2. Add column constant after COL_LABEL:
     ```
     var COL_PARENT_EMAIL = 28; // AB — Parent/Guardian Email (Phase 5.1)
     ```

  3. Update the COLUMN INDEX REFERENCE comment block to include AB.

  **COLUMN-MAP.md:**

  1. Add row to the Column Map table after AA:
     ```
     | AB | 28 | 27 | Parent/Guardian Email | Referee form | Phase 5.1 |
     ```

  2. Add to Column Writer Summary — update the "Referee form" row to include AB in the Columns list.

  3. Update the "Last updated" line to 2026-03-23.

  4. Add a section note: "Column AB note: Written by the referee detail form only when referee age < 18. Blank for adult referees."
  </action>
  <verify>
  - `grep 'Parent/Guardian Email' scripts/setup-schema-v2.gs` returns the HEADERS_V2 entry
  - `grep 'parentEmail' scripts/adminemail.gs` returns the nominee mapping line
  - `grep 'age.*r\[9\]' scripts/adminemail.gs` returns the age field in nominee mapping
  - `grep 'AB' .planning/COLUMN-MAP.md` returns the new column row
  - `grep '1, 28' scripts/setup-schema-v2.gs` confirms expanded header range
  </verify>
  <done>
  - adminemail.gs getAllNominees reads 28 columns and returns age + parentEmail per nominee
  - setup-schema-v2.gs HEADERS_V2 includes AB header, ranges expanded to 28 columns
  - COLUMN-MAP.md documents column AB with writer, phase, and usage note
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c 'COL_PARENT_EMAIL' scripts/refdetails.gs scripts/setup-schema-v2.gs` — should show 1+ match per file
2. `grep 'parentEmail' scripts/refdetails.gs scripts/adminemail.gs` — should show matches in both files
3. `grep 'Parent/Guardian Email' scripts/setup-schema-v2.gs .planning/COLUMN-MAP.md` — should show matches in both files
4. `grep '1, 28' scripts/refdetails.gs scripts/adminemail.gs scripts/setup-schema-v2.gs` — should show expanded ranges in all three
5. No syntax errors in .gs files (var declarations, commas, brackets all balanced)
</verification>

<success_criteria>
- COL_PARENT_EMAIL = 28 declared in refdetails.gs and setup-schema-v2.gs
- _handleGetDetails returns parentEmail from rowData[27] after reading 28 columns
- _handleSubmitDetails writes payload.parentEmail to column AB
- _handleGetAllNominees returns age and parentEmail per nominee after reading 28 columns
- HEADERS_V2 array includes 'Parent/Guardian Email' at the correct position
- COLUMN-MAP.md includes AB row and updated writer summary
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-parent-guardian-email-for-minors/05.1-01-SUMMARY.md`
</output>
