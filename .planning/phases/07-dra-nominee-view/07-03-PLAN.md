---
phase: 07-dra-nominee-view
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: [spring-state-cup-nomination.html]
autonomous: true

must_haves:
  truths:
    - "A visible 'Review your nominees' link appears at the top of the nomination form, above the form fields"
    - "The link navigates to dra-nominees.html (relative URL)"
    - "The link is styled consistently with the project design system"
    - "The link does not interfere with any existing form functionality"
  artifacts:
    - path: "spring-state-cup-nomination.html"
      provides: "Review your nominees link pointing to dra-nominees.html"
      contains: "dra-nominees.html"
  key_links:
    - from: "spring-state-cup-nomination.html link"
      to: "dra-nominees.html"
      via: "HTML anchor tag with relative URL"
      pattern: "dra-nominees\\.html"
---

<objective>
Add a "Review your nominees" link to the top of the DRA nomination form that navigates to the DRA nominee view page (DRA-05).

Purpose: DRAs who have previously submitted nominations can quickly check the status and details of their referees without having to ask the assignor. The link is placed prominently at the top of the form they already visit.

Output: Updated spring-state-cup-nomination.html with a visible link to dra-nominees.html.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dra-nominee-view/07-CONTEXT.md
@.planning/phases/07-dra-nominee-view/07-RESEARCH.md
@spring-state-cup-nomination.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Review your nominees" link to the top of the nomination form</name>
  <files>spring-state-cup-nomination.html</files>
  <action>
  Add a link to the DRA nominee view page at the top of the `<main class="main">` section, BEFORE the first `.sec-head` section heading. The link should be the first element inside `<main>`.

  **HTML to add** (insert right after `<main class="main">`):
  ```html
  <div class="review-link-bar">
    <a href="dra-nominees.html" class="review-link">Review your nominees &rarr;</a>
  </div>
  ```

  **CSS to add** (in the `<style>` block, before the closing `</style>` tag):
  ```css
  /* review nominees link */
  .review-link-bar {
    margin-bottom: 24px;
    padding: 12px 16px;
    background: var(--white);
    border: 1px solid var(--border);
    border-radius: var(--r);
    border-left: 3px solid var(--navy);
  }
  .review-link {
    color: var(--navy);
    font-size: 14px;
    font-weight: 600;
    text-decoration: none;
    transition: color .18s;
  }
  .review-link:hover {
    color: var(--red);
  }
  ```

  This creates a subtle but prominent card-style link bar at the top of the form. The navy left border draws the eye without being intrusive. The arrow hint indicates navigation to another page.

  **Do NOT modify:**
  - Any existing form fields or sections
  - Any JavaScript functionality
  - Any .gs files
  </action>
  <verify>
  - Link appears as the first element inside `<main class="main">`
  - Link text is "Review your nominees" with an arrow
  - Link href is "dra-nominees.html" (relative URL)
  - Link styling uses project design system variables
  - No existing form fields, sections, or JavaScript modified
  - No .gs files modified
  </verify>
  <done>
  - "Review your nominees" link added at top of nomination form
  - Link navigates to dra-nominees.html
  - Styled with navy left border card, navy text, hover turns red
  - Positioned before all form sections
  - No existing functionality affected
  </done>
</task>

</tasks>

<verification>
- spring-state-cup-nomination.html contains a link to dra-nominees.html
- Link is visible at the top of the main content area, before form sections
- Link text reads "Review your nominees" with arrow indicator
- Link uses relative URL (dra-nominees.html)
- Styling uses existing CSS variables (--navy, --white, --border, --r, --red)
- No existing form fields, JavaScript, or .gs files modified
</verification>

<success_criteria>
1. "Review your nominees" link is visible at the top of the nomination form
2. Clicking it navigates to dra-nominees.html
3. Link styling matches the project design system
4. No existing form functionality is affected
</success_criteria>

<output>
After completion, create `.planning/phases/07-dra-nominee-view/07-03-SUMMARY.md`
</output>
